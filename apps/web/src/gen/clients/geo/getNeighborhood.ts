/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	GetNeighborhoodQueryResponse,
	GetNeighborhoodPathParams,
	GetNeighborhood404,
} from "../../types/geo/GetNeighborhood";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { getNeighborhoodQueryResponseSchema } from "../../schemas/geo/getNeighborhoodSchema";

function getGetNeighborhoodUrl({
	id,
}: {
	id: GetNeighborhoodPathParams["id"];
}) {
	const res = { method: "GET", url: `/neighborhoods/${id}` as const };
	return res;
}

/**
 * @summary Get a neighborhood by id
 * {@link /neighborhoods/:id}
 */
export async function getNeighborhood(
	{ id }: { id: GetNeighborhoodPathParams["id"] },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		GetNeighborhoodQueryResponse,
		ResponseErrorConfig<GetNeighborhood404>,
		unknown
	>({
		method: "GET",
		url: getGetNeighborhoodUrl({ id }).url.toString(),
		...requestConfig,
	});
	return getNeighborhoodQueryResponseSchema.parse(res.data);
}
