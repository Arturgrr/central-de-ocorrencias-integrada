/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	GetCityQueryResponse,
	GetCityPathParams,
	GetCity404,
} from "../../types/geo/GetCity";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { getCityQueryResponseSchema } from "../../schemas/geo/getCitySchema";

function getGetCityUrl({ id }: { id: GetCityPathParams["id"] }) {
	const res = { method: "GET", url: `/api/cities/${id}` as const };
	return res;
}

/**
 * @summary Get a city by id
 * {@link /api/cities/:id}
 */
export async function getCity(
	{ id }: { id: GetCityPathParams["id"] },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		GetCityQueryResponse,
		ResponseErrorConfig<GetCity404>,
		unknown
	>({
		method: "GET",
		url: getGetCityUrl({ id }).url.toString(),
		...requestConfig,
	});
	return getCityQueryResponseSchema.parse(res.data);
}
