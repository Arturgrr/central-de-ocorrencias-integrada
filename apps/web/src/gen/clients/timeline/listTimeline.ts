/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	ListTimelineQueryResponse,
	ListTimelinePathParams,
} from "../../types/timeline/ListTimeline";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { listTimelineQueryResponseSchema } from "../../schemas/timeline/listTimelineSchema";

function getListTimelineUrl({ id }: { id: ListTimelinePathParams["id"] }) {
	const res = {
		method: "GET",
		url: `/api/occurrences/${id}/timeline` as const,
	};
	return res;
}

/**
 * @summary List timeline events for an occurrence
 * {@link /api/occurrences/:id/timeline}
 */
export async function listTimeline(
	{ id }: { id: ListTimelinePathParams["id"] },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		ListTimelineQueryResponse,
		ResponseErrorConfig<Error>,
		unknown
	>({
		method: "GET",
		url: getListTimelineUrl({ id }).url.toString(),
		...requestConfig,
	});
	return listTimelineQueryResponseSchema.parse(res.data);
}
