/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type { GetMeQueryResponse, GetMe401 } from "../../types/users/GetMe";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { getMeQueryResponseSchema } from "../../schemas/users/getMeSchema";

function getGetMeUrl() {
	const res = { method: "GET", url: `/api/me` as const };
	return res;
}

/**
 * @summary Get the authenticated user profile
 * {@link /api/me}
 */
export async function getMe(
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		GetMeQueryResponse,
		ResponseErrorConfig<GetMe401>,
		unknown
	>({ method: "GET", url: getGetMeUrl().url.toString(), ...requestConfig });
	return getMeQueryResponseSchema.parse(res.data);
}
