/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type { ListAgentsQueryResponse } from "../../types/users/ListAgents";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { listAgentsQueryResponseSchema } from "../../schemas/users/listAgentsSchema";

function getListAgentsUrl() {
	const res = { method: "GET", url: `/agents` as const };
	return res;
}

/**
 * @summary List agents
 * {@link /agents}
 */
export async function listAgents(
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		ListAgentsQueryResponse,
		ResponseErrorConfig<Error>,
		unknown
	>({
		method: "GET",
		url: getListAgentsUrl().url.toString(),
		...requestConfig,
	});
	return listAgentsQueryResponseSchema.parse(res.data);
}
