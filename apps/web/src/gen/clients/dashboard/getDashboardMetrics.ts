/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type { GetDashboardMetricsQueryResponse } from "../../types/dashboard/GetDashboardMetrics";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { getDashboardMetricsQueryResponseSchema } from "../../schemas/dashboard/getDashboardMetricsSchema";

function getGetDashboardMetricsUrl() {
	const res = { method: "GET", url: `/dashboard/metrics` as const };
	return res;
}

/**
 * @summary Get aggregate metrics for the operations panel
 * {@link /dashboard/metrics}
 */
export async function getDashboardMetrics(
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		GetDashboardMetricsQueryResponse,
		ResponseErrorConfig<Error>,
		unknown
	>({
		method: "GET",
		url: getGetDashboardMetricsUrl().url.toString(),
		...requestConfig,
	});
	return getDashboardMetricsQueryResponseSchema.parse(res.data);
}
