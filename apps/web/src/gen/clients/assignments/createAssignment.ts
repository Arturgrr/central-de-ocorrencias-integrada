/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	CreateAssignmentMutationRequest,
	CreateAssignmentMutationResponse,
	CreateAssignmentPathParams,
} from "../../types/assignments/CreateAssignment";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import {
	createAssignmentMutationResponseSchema,
	createAssignmentMutationRequestSchema,
} from "../../schemas/assignments/createAssignmentSchema";

function getCreateAssignmentUrl({
	id,
}: {
	id: CreateAssignmentPathParams["id"];
}) {
	const res = {
		method: "POST",
		url: `/api/occurrences/${id}/assignments` as const,
	};
	return res;
}

/**
 * @summary Create an assignment for an occurrence
 * {@link /api/occurrences/:id/assignments}
 */
export async function createAssignment(
	{
		id,
		data,
	}: {
		id: CreateAssignmentPathParams["id"];
		data: CreateAssignmentMutationRequest;
	},
	config: Partial<RequestConfig<CreateAssignmentMutationRequest>> & {
		client?: Client;
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const requestData = createAssignmentMutationRequestSchema.parse(data);

	const res = await request<
		CreateAssignmentMutationResponse,
		ResponseErrorConfig<Error>,
		CreateAssignmentMutationRequest
	>({
		method: "POST",
		url: getCreateAssignmentUrl({ id }).url.toString(),
		data: requestData,
		...requestConfig,
	});
	return createAssignmentMutationResponseSchema.parse(res.data);
}
