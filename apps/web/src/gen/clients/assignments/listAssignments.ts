/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	ListAssignmentsQueryResponse,
	ListAssignmentsPathParams,
} from "../../types/assignments/ListAssignments";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { listAssignmentsQueryResponseSchema } from "../../schemas/assignments/listAssignmentsSchema";

function getListAssignmentsUrl({
	id,
}: {
	id: ListAssignmentsPathParams["id"];
}) {
	const res = { method: "GET", url: `/occurrences/${id}/assignments` as const };
	return res;
}

/**
 * @summary List assignments for an occurrence
 * {@link /occurrences/:id/assignments}
 */
export async function listAssignments(
	{ id }: { id: ListAssignmentsPathParams["id"] },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		ListAssignmentsQueryResponse,
		ResponseErrorConfig<Error>,
		unknown
	>({
		method: "GET",
		url: getListAssignmentsUrl({ id }).url.toString(),
		...requestConfig,
	});
	return listAssignmentsQueryResponseSchema.parse(res.data);
}
