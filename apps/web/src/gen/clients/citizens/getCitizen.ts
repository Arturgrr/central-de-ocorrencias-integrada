/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	GetCitizenQueryResponse,
	GetCitizenPathParams,
	GetCitizen404,
} from "../../types/citizens/GetCitizen";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { getCitizenQueryResponseSchema } from "../../schemas/citizens/getCitizenSchema";

function getGetCitizenUrl({ id }: { id: GetCitizenPathParams["id"] }) {
	const res = { method: "GET", url: `/api/citizens/${id}` as const };
	return res;
}

/**
 * @summary Get a citizen by id
 * {@link /api/citizens/:id}
 */
export async function getCitizen(
	{ id }: { id: GetCitizenPathParams["id"] },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		GetCitizenQueryResponse,
		ResponseErrorConfig<GetCitizen404>,
		unknown
	>({
		method: "GET",
		url: getGetCitizenUrl({ id }).url.toString(),
		...requestConfig,
	});
	return getCitizenQueryResponseSchema.parse(res.data);
}
