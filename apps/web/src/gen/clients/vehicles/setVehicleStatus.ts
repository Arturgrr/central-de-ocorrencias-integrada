/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	SetVehicleStatusMutationRequest,
	SetVehicleStatusMutationResponse,
	SetVehicleStatusPathParams,
	SetVehicleStatus404,
} from "../../types/vehicles/SetVehicleStatus";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import {
	setVehicleStatusMutationResponseSchema,
	setVehicleStatusMutationRequestSchema,
} from "../../schemas/vehicles/setVehicleStatusSchema";

function getSetVehicleStatusUrl({
	id,
}: {
	id: SetVehicleStatusPathParams["id"];
}) {
	const res = { method: "PATCH", url: `/api/vehicles/${id}/status` as const };
	return res;
}

/**
 * @summary Update a vehicle's status
 * {@link /api/vehicles/:id/status}
 */
export async function setVehicleStatus(
	{
		id,
		data,
	}: {
		id: SetVehicleStatusPathParams["id"];
		data: SetVehicleStatusMutationRequest;
	},
	config: Partial<RequestConfig<SetVehicleStatusMutationRequest>> & {
		client?: Client;
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const requestData = setVehicleStatusMutationRequestSchema.parse(data);

	const res = await request<
		SetVehicleStatusMutationResponse,
		ResponseErrorConfig<SetVehicleStatus404>,
		SetVehicleStatusMutationRequest
	>({
		method: "PATCH",
		url: getSetVehicleStatusUrl({ id }).url.toString(),
		data: requestData,
		...requestConfig,
	});
	return setVehicleStatusMutationResponseSchema.parse(res.data);
}
