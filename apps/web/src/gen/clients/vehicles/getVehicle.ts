/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	GetVehicleQueryResponse,
	GetVehiclePathParams,
	GetVehicle404,
} from "../../types/vehicles/GetVehicle";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { getVehicleQueryResponseSchema } from "../../schemas/vehicles/getVehicleSchema";

function getGetVehicleUrl({ id }: { id: GetVehiclePathParams["id"] }) {
	const res = { method: "GET", url: `/vehicles/${id}` as const };
	return res;
}

/**
 * @summary Get a vehicle by id
 * {@link /vehicles/:id}
 */
export async function getVehicle(
	{ id }: { id: GetVehiclePathParams["id"] },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		GetVehicleQueryResponse,
		ResponseErrorConfig<GetVehicle404>,
		unknown
	>({
		method: "GET",
		url: getGetVehicleUrl({ id }).url.toString(),
		...requestConfig,
	});
	return getVehicleQueryResponseSchema.parse(res.data);
}
