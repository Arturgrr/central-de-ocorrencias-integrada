/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	ReportVehicleLocationMutationRequest,
	ReportVehicleLocationMutationResponse,
	ReportVehicleLocationPathParams,
	ReportVehicleLocation404,
} from "../../types/vehicles/ReportVehicleLocation";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import {
	reportVehicleLocationMutationResponseSchema,
	reportVehicleLocationMutationRequestSchema,
} from "../../schemas/vehicles/reportVehicleLocationSchema";

function getReportVehicleLocationUrl({
	id,
}: {
	id: ReportVehicleLocationPathParams["id"];
}) {
	const res = { method: "POST", url: `/vehicles/${id}/locations` as const };
	return res;
}

/**
 * @summary Report a vehicle's location
 * {@link /vehicles/:id/locations}
 */
export async function reportVehicleLocation(
	{
		id,
		data,
	}: {
		id: ReportVehicleLocationPathParams["id"];
		data: ReportVehicleLocationMutationRequest;
	},
	config: Partial<RequestConfig<ReportVehicleLocationMutationRequest>> & {
		client?: Client;
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const requestData = reportVehicleLocationMutationRequestSchema.parse(data);

	const res = await request<
		ReportVehicleLocationMutationResponse,
		ResponseErrorConfig<ReportVehicleLocation404>,
		ReportVehicleLocationMutationRequest
	>({
		method: "POST",
		url: getReportVehicleLocationUrl({ id }).url.toString(),
		data: requestData,
		...requestConfig,
	});
	return reportVehicleLocationMutationResponseSchema.parse(res.data);
}
