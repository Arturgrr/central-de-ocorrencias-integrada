/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	UpdateVehicleMutationRequest,
	UpdateVehicleMutationResponse,
	UpdateVehiclePathParams,
	UpdateVehicle404,
} from "../../types/vehicles/UpdateVehicle";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import {
	updateVehicleMutationResponseSchema,
	updateVehicleMutationRequestSchema,
} from "../../schemas/vehicles/updateVehicleSchema";

function getUpdateVehicleUrl({ id }: { id: UpdateVehiclePathParams["id"] }) {
	const res = { method: "PUT", url: `/api/vehicles/${id}` as const };
	return res;
}

/**
 * @summary Update a vehicle
 * {@link /api/vehicles/:id}
 */
export async function updateVehicle(
	{
		id,
		data,
	}: { id: UpdateVehiclePathParams["id"]; data: UpdateVehicleMutationRequest },
	config: Partial<RequestConfig<UpdateVehicleMutationRequest>> & {
		client?: Client;
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const requestData = updateVehicleMutationRequestSchema.parse(data);

	const res = await request<
		UpdateVehicleMutationResponse,
		ResponseErrorConfig<UpdateVehicle404>,
		UpdateVehicleMutationRequest
	>({
		method: "PUT",
		url: getUpdateVehicleUrl({ id }).url.toString(),
		data: requestData,
		...requestConfig,
	});
	return updateVehicleMutationResponseSchema.parse(res.data);
}
