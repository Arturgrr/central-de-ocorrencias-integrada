/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	CloseOccurrenceMutationResponse,
	CloseOccurrencePathParams,
	CloseOccurrence404,
} from "../../types/occurrences/CloseOccurrence";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { closeOccurrenceMutationResponseSchema } from "../../schemas/occurrences/closeOccurrenceSchema";

function getCloseOccurrenceUrl({
	id,
}: {
	id: CloseOccurrencePathParams["id"];
}) {
	const res = { method: "PATCH", url: `/occurrences/${id}/close` as const };
	return res;
}

/**
 * @summary Close an occurrence
 * {@link /occurrences/:id/close}
 */
export async function closeOccurrence(
	{ id }: { id: CloseOccurrencePathParams["id"] },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		CloseOccurrenceMutationResponse,
		ResponseErrorConfig<CloseOccurrence404>,
		unknown
	>({
		method: "PATCH",
		url: getCloseOccurrenceUrl({ id }).url.toString(),
		...requestConfig,
	});
	return closeOccurrenceMutationResponseSchema.parse(res.data);
}
