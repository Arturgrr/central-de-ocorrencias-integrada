/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	GetOccurrenceQueryResponse,
	GetOccurrencePathParams,
	GetOccurrence404,
} from "../../types/occurrences/GetOccurrence";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { getOccurrenceQueryResponseSchema } from "../../schemas/occurrences/getOccurrenceSchema";

function getGetOccurrenceUrl({ id }: { id: GetOccurrencePathParams["id"] }) {
	const res = { method: "GET", url: `/api/occurrences/${id}` as const };
	return res;
}

/**
 * @summary Get an occurrence by id
 * {@link /api/occurrences/:id}
 */
export async function getOccurrence(
	{ id }: { id: GetOccurrencePathParams["id"] },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		GetOccurrenceQueryResponse,
		ResponseErrorConfig<GetOccurrence404>,
		unknown
	>({
		method: "GET",
		url: getGetOccurrenceUrl({ id }).url.toString(),
		...requestConfig,
	});
	return getOccurrenceQueryResponseSchema.parse(res.data);
}
