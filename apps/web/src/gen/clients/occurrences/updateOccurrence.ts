/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	UpdateOccurrenceMutationRequest,
	UpdateOccurrenceMutationResponse,
	UpdateOccurrencePathParams,
	UpdateOccurrence404,
} from "../../types/occurrences/UpdateOccurrence";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import {
	updateOccurrenceMutationResponseSchema,
	updateOccurrenceMutationRequestSchema,
} from "../../schemas/occurrences/updateOccurrenceSchema";

function getUpdateOccurrenceUrl({
	id,
}: {
	id: UpdateOccurrencePathParams["id"];
}) {
	const res = { method: "PUT", url: `/occurrences/${id}` as const };
	return res;
}

/**
 * @summary Update an occurrence
 * {@link /occurrences/:id}
 */
export async function updateOccurrence(
	{
		id,
		data,
	}: {
		id: UpdateOccurrencePathParams["id"];
		data: UpdateOccurrenceMutationRequest;
	},
	config: Partial<RequestConfig<UpdateOccurrenceMutationRequest>> & {
		client?: Client;
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const requestData = updateOccurrenceMutationRequestSchema.parse(data);

	const res = await request<
		UpdateOccurrenceMutationResponse,
		ResponseErrorConfig<UpdateOccurrence404>,
		UpdateOccurrenceMutationRequest
	>({
		method: "PUT",
		url: getUpdateOccurrenceUrl({ id }).url.toString(),
		data: requestData,
		...requestConfig,
	});
	return updateOccurrenceMutationResponseSchema.parse(res.data);
}
