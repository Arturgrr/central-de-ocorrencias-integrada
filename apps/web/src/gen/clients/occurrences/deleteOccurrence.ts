/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	DeleteOccurrenceMutationResponse,
	DeleteOccurrencePathParams,
	DeleteOccurrence404,
} from "../../types/occurrences/DeleteOccurrence";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { deleteOccurrenceMutationResponseSchema } from "../../schemas/occurrences/deleteOccurrenceSchema";

function getDeleteOccurrenceUrl({
	id,
}: {
	id: DeleteOccurrencePathParams["id"];
}) {
	const res = { method: "DELETE", url: `/api/occurrences/${id}` as const };
	return res;
}

/**
 * @summary Delete an occurrence
 * {@link /api/occurrences/:id}
 */
export async function deleteOccurrence(
	{ id }: { id: DeleteOccurrencePathParams["id"] },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		DeleteOccurrenceMutationResponse,
		ResponseErrorConfig<DeleteOccurrence404>,
		unknown
	>({
		method: "DELETE",
		url: getDeleteOccurrenceUrl({ id }).url.toString(),
		...requestConfig,
	});
	return deleteOccurrenceMutationResponseSchema.parse(res.data);
}
