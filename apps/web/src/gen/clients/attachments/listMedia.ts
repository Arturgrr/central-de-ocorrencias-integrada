/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	ListMediaQueryResponse,
	ListMediaQueryParams,
} from "../../types/attachments/ListMedia";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { listMediaQueryResponseSchema } from "../../schemas/attachments/listMediaSchema";

function getListMediaUrl() {
	const res = { method: "GET", url: `/attachments` as const };
	return res;
}

/**
 * @summary List every attachment of the media library
 * {@link /attachments}
 */
export async function listMedia(
	{ params }: { params?: ListMediaQueryParams } = {},
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		ListMediaQueryResponse,
		ResponseErrorConfig<Error>,
		unknown
	>({
		method: "GET",
		url: getListMediaUrl().url.toString(),
		params,
		...requestConfig,
	});
	return listMediaQueryResponseSchema.parse(res.data);
}
