/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api-client";
import type {
	ListAttachmentsQueryResponse,
	ListAttachmentsPathParams,
} from "../../types/attachments/ListAttachments";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import { listAttachmentsQueryResponseSchema } from "../../schemas/attachments/listAttachmentsSchema";

function getListAttachmentsUrl({
	id,
}: {
	id: ListAttachmentsPathParams["id"];
}) {
	const res = { method: "GET", url: `/occurrences/${id}/attachments` as const };
	return res;
}

/**
 * @summary List attachments for an occurrence
 * {@link /occurrences/:id/attachments}
 */
export async function listAttachments(
	{ id }: { id: ListAttachmentsPathParams["id"] },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		ListAttachmentsQueryResponse,
		ResponseErrorConfig<Error>,
		unknown
	>({
		method: "GET",
		url: getListAttachmentsUrl({ id }).url.toString(),
		...requestConfig,
	});
	return listAttachmentsQueryResponseSchema.parse(res.data);
}
