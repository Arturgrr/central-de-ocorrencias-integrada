/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	ListOccurrencesQueryResponse,
	ListOccurrencesQueryParams,
} from "../../types/occurrences/ListOccurrences";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { listOccurrences } from "../../clients/occurrences/listOccurrences";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listOccurrencesQueryKey = (params?: ListOccurrencesQueryParams) =>
	[{ url: "/occurrences" }, ...(params ? [params] : [])] as const;

export type ListOccurrencesQueryKey = ReturnType<
	typeof listOccurrencesQueryKey
>;

export function listOccurrencesQueryOptions(
	{ params }: { params?: ListOccurrencesQueryParams } = {},
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = listOccurrencesQueryKey(params);
	return queryOptions<
		ListOccurrencesQueryResponse,
		ResponseErrorConfig<Error>,
		ListOccurrencesQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return listOccurrences(
				{ params: params },
				{ ...config, signal: config.signal ?? signal },
			);
		},
	});
}

/**
 * @summary List occurrences
 * {@link /occurrences}
 */
export function useListOccurrences<
	TData = ListOccurrencesQueryResponse,
	TQueryData = ListOccurrencesQueryResponse,
	TQueryKey extends QueryKey = ListOccurrencesQueryKey,
>(
	{ params }: { params?: ListOccurrencesQueryParams } = {},
	options: {
		query?: Partial<
			QueryObserverOptions<
				ListOccurrencesQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? listOccurrencesQueryKey(params);

	const query = useQuery(
		{
			...listOccurrencesQueryOptions({ params }, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
