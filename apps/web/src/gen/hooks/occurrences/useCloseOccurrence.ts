/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	CloseOccurrenceMutationResponse,
	CloseOccurrencePathParams,
	CloseOccurrence404,
} from "../../types/occurrences/CloseOccurrence";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	UseMutationOptions,
	UseMutationResult,
	QueryClient,
} from "@tanstack/react-query";
import { closeOccurrence } from "../../clients/occurrences/closeOccurrence";
import { mutationOptions, useMutation } from "@tanstack/react-query";

export const closeOccurrenceMutationKey = () =>
	[{ url: "/api/occurrences/:id/close" }] as const;

export type CloseOccurrenceMutationKey = ReturnType<
	typeof closeOccurrenceMutationKey
>;

export function closeOccurrenceMutationOptions<TContext = unknown>(
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const mutationKey = closeOccurrenceMutationKey();
	return mutationOptions<
		CloseOccurrenceMutationResponse,
		ResponseErrorConfig<CloseOccurrence404>,
		{ id: CloseOccurrencePathParams["id"] },
		TContext
	>({
		mutationKey,
		mutationFn: async ({ id }) => {
			return closeOccurrence({ id }, config);
		},
	});
}

/**
 * @summary Close an occurrence
 * {@link /api/occurrences/:id/close}
 */
export function useCloseOccurrence<TContext>(
	options: {
		mutation?: UseMutationOptions<
			CloseOccurrenceMutationResponse,
			ResponseErrorConfig<CloseOccurrence404>,
			{ id: CloseOccurrencePathParams["id"] },
			TContext
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { mutation = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...mutationOptions } = mutation;
	const mutationKey =
		mutationOptions.mutationKey ?? closeOccurrenceMutationKey();

	const baseOptions = closeOccurrenceMutationOptions(
		config,
	) as UseMutationOptions<
		CloseOccurrenceMutationResponse,
		ResponseErrorConfig<CloseOccurrence404>,
		{ id: CloseOccurrencePathParams["id"] },
		TContext
	>;

	return useMutation<
		CloseOccurrenceMutationResponse,
		ResponseErrorConfig<CloseOccurrence404>,
		{ id: CloseOccurrencePathParams["id"] },
		TContext
	>(
		{
			...baseOptions,
			mutationKey,
			...mutationOptions,
		},
		queryClient,
	) as UseMutationResult<
		CloseOccurrenceMutationResponse,
		ResponseErrorConfig<CloseOccurrence404>,
		{ id: CloseOccurrencePathParams["id"] },
		TContext
	>;
}
