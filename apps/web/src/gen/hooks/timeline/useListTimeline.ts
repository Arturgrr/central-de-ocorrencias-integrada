/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	ListTimelineQueryResponse,
	ListTimelinePathParams,
} from "../../types/timeline/ListTimeline";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { listTimeline } from "../../clients/timeline/listTimeline";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listTimelineQueryKey = ({
	id,
}: {
	id: ListTimelinePathParams["id"] | undefined;
}) => [{ url: "/occurrences/:id/timeline", params: { id: id } }] as const;

export type ListTimelineQueryKey = ReturnType<typeof listTimelineQueryKey>;

export function listTimelineQueryOptions(
	{ id }: { id: ListTimelinePathParams["id"] | undefined },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = listTimelineQueryKey({ id });
	return queryOptions<
		ListTimelineQueryResponse,
		ResponseErrorConfig<Error>,
		ListTimelineQueryResponse,
		typeof queryKey
	>({
		enabled: !!id,
		queryKey,
		queryFn: async ({ signal }) => {
			return listTimeline(
				{ id: id! },
				{ ...config, signal: config.signal ?? signal },
			);
		},
	});
}

/**
 * @summary List timeline events for an occurrence
 * {@link /occurrences/:id/timeline}
 */
export function useListTimeline<
	TData = ListTimelineQueryResponse,
	TQueryData = ListTimelineQueryResponse,
	TQueryKey extends QueryKey = ListTimelineQueryKey,
>(
	{ id }: { id: ListTimelinePathParams["id"] | undefined },
	options: {
		query?: Partial<
			QueryObserverOptions<
				ListTimelineQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? listTimelineQueryKey({ id });

	const query = useQuery(
		{
			...listTimelineQueryOptions({ id }, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
