/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	GetCitizenQueryResponse,
	GetCitizenPathParams,
	GetCitizen404,
} from "../../types/citizens/GetCitizen";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { getCitizen } from "../../clients/citizens/getCitizen";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getCitizenQueryKey = ({
	id,
}: {
	id: GetCitizenPathParams["id"] | undefined;
}) => [{ url: "/api/citizens/:id", params: { id: id } }] as const;

export type GetCitizenQueryKey = ReturnType<typeof getCitizenQueryKey>;

export function getCitizenQueryOptions(
	{ id }: { id: GetCitizenPathParams["id"] | undefined },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getCitizenQueryKey({ id });
	return queryOptions<
		GetCitizenQueryResponse,
		ResponseErrorConfig<GetCitizen404>,
		GetCitizenQueryResponse,
		typeof queryKey
	>({
		enabled: !!id,
		queryKey,
		queryFn: async ({ signal }) => {
			return getCitizen(
				{ id: id! },
				{ ...config, signal: config.signal ?? signal },
			);
		},
	});
}

/**
 * @summary Get a citizen by id
 * {@link /api/citizens/:id}
 */
export function useGetCitizen<
	TData = GetCitizenQueryResponse,
	TQueryData = GetCitizenQueryResponse,
	TQueryKey extends QueryKey = GetCitizenQueryKey,
>(
	{ id }: { id: GetCitizenPathParams["id"] | undefined },
	options: {
		query?: Partial<
			QueryObserverOptions<
				GetCitizenQueryResponse,
				ResponseErrorConfig<GetCitizen404>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getCitizenQueryKey({ id });

	const query = useQuery(
		{
			...getCitizenQueryOptions({ id }, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<GetCitizen404>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
