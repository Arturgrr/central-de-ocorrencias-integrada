/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	ListAssignmentsQueryResponse,
	ListAssignmentsPathParams,
} from "../../types/assignments/ListAssignments";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { listAssignments } from "../../clients/assignments/listAssignments";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listAssignmentsQueryKey = ({
	id,
}: {
	id: ListAssignmentsPathParams["id"] | undefined;
}) =>
	[{ url: "/api/occurrences/:id/assignments", params: { id: id } }] as const;

export type ListAssignmentsQueryKey = ReturnType<
	typeof listAssignmentsQueryKey
>;

export function listAssignmentsQueryOptions(
	{ id }: { id: ListAssignmentsPathParams["id"] | undefined },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = listAssignmentsQueryKey({ id });
	return queryOptions<
		ListAssignmentsQueryResponse,
		ResponseErrorConfig<Error>,
		ListAssignmentsQueryResponse,
		typeof queryKey
	>({
		enabled: !!id,
		queryKey,
		queryFn: async ({ signal }) => {
			return listAssignments(
				{ id: id! },
				{ ...config, signal: config.signal ?? signal },
			);
		},
	});
}

/**
 * @summary List assignments for an occurrence
 * {@link /api/occurrences/:id/assignments}
 */
export function useListAssignments<
	TData = ListAssignmentsQueryResponse,
	TQueryData = ListAssignmentsQueryResponse,
	TQueryKey extends QueryKey = ListAssignmentsQueryKey,
>(
	{ id }: { id: ListAssignmentsPathParams["id"] | undefined },
	options: {
		query?: Partial<
			QueryObserverOptions<
				ListAssignmentsQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? listAssignmentsQueryKey({ id });

	const query = useQuery(
		{
			...listAssignmentsQueryOptions({ id }, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
