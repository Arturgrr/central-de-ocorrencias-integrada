/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	ListMediaQueryResponse,
	ListMediaQueryParams,
} from "../../types/attachments/ListMedia";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { listMedia } from "../../clients/attachments/listMedia";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listMediaQueryKey = (params?: ListMediaQueryParams) =>
	[{ url: "/attachments" }, ...(params ? [params] : [])] as const;

export type ListMediaQueryKey = ReturnType<typeof listMediaQueryKey>;

export function listMediaQueryOptions(
	{ params }: { params?: ListMediaQueryParams } = {},
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = listMediaQueryKey(params);
	return queryOptions<
		ListMediaQueryResponse,
		ResponseErrorConfig<Error>,
		ListMediaQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return listMedia(
				{ params: params },
				{ ...config, signal: config.signal ?? signal },
			);
		},
	});
}

/**
 * @summary List every attachment of the media library
 * {@link /attachments}
 */
export function useListMedia<
	TData = ListMediaQueryResponse,
	TQueryData = ListMediaQueryResponse,
	TQueryKey extends QueryKey = ListMediaQueryKey,
>(
	{ params }: { params?: ListMediaQueryParams } = {},
	options: {
		query?: Partial<
			QueryObserverOptions<
				ListMediaQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? listMediaQueryKey(params);

	const query = useQuery(
		{
			...listMediaQueryOptions({ params }, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
