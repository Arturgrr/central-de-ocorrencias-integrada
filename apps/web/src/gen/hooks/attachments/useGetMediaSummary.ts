/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetMediaSummaryQueryResponse } from "../../types/attachments/GetMediaSummary";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { getMediaSummary } from "../../clients/attachments/getMediaSummary";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getMediaSummaryQueryKey = () =>
	[{ url: "/attachments/summary" }] as const;

export type GetMediaSummaryQueryKey = ReturnType<
	typeof getMediaSummaryQueryKey
>;

export function getMediaSummaryQueryOptions(
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getMediaSummaryQueryKey();
	return queryOptions<
		GetMediaSummaryQueryResponse,
		ResponseErrorConfig<Error>,
		GetMediaSummaryQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return getMediaSummary({ ...config, signal: config.signal ?? signal });
		},
	});
}

/**
 * @summary Count attachments grouped by kind
 * {@link /attachments/summary}
 */
export function useGetMediaSummary<
	TData = GetMediaSummaryQueryResponse,
	TQueryData = GetMediaSummaryQueryResponse,
	TQueryKey extends QueryKey = GetMediaSummaryQueryKey,
>(
	options: {
		query?: Partial<
			QueryObserverOptions<
				GetMediaSummaryQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getMediaSummaryQueryKey();

	const query = useQuery(
		{
			...getMediaSummaryQueryOptions(config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
