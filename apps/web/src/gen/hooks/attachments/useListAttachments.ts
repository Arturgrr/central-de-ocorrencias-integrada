/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	ListAttachmentsQueryResponse,
	ListAttachmentsPathParams,
} from "../../types/attachments/ListAttachments";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { listAttachments } from "../../clients/attachments/listAttachments";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listAttachmentsQueryKey = ({
	id,
}: {
	id: ListAttachmentsPathParams["id"] | undefined;
}) => [{ url: "/occurrences/:id/attachments", params: { id: id } }] as const;

export type ListAttachmentsQueryKey = ReturnType<
	typeof listAttachmentsQueryKey
>;

export function listAttachmentsQueryOptions(
	{ id }: { id: ListAttachmentsPathParams["id"] | undefined },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = listAttachmentsQueryKey({ id });
	return queryOptions<
		ListAttachmentsQueryResponse,
		ResponseErrorConfig<Error>,
		ListAttachmentsQueryResponse,
		typeof queryKey
	>({
		enabled: !!id,
		queryKey,
		queryFn: async ({ signal }) => {
			return listAttachments(
				{ id: id! },
				{ ...config, signal: config.signal ?? signal },
			);
		},
	});
}

/**
 * @summary List attachments for an occurrence
 * {@link /occurrences/:id/attachments}
 */
export function useListAttachments<
	TData = ListAttachmentsQueryResponse,
	TQueryData = ListAttachmentsQueryResponse,
	TQueryKey extends QueryKey = ListAttachmentsQueryKey,
>(
	{ id }: { id: ListAttachmentsPathParams["id"] | undefined },
	options: {
		query?: Partial<
			QueryObserverOptions<
				ListAttachmentsQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? listAttachmentsQueryKey({ id });

	const query = useQuery(
		{
			...listAttachmentsQueryOptions({ id }, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
