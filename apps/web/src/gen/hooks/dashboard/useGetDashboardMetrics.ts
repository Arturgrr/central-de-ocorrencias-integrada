/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetDashboardMetricsQueryResponse } from "../../types/dashboard/GetDashboardMetrics";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { getDashboardMetrics } from "../../clients/dashboard/getDashboardMetrics";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getDashboardMetricsQueryKey = () =>
	[{ url: "/api/dashboard/metrics" }] as const;

export type GetDashboardMetricsQueryKey = ReturnType<
	typeof getDashboardMetricsQueryKey
>;

export function getDashboardMetricsQueryOptions(
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getDashboardMetricsQueryKey();
	return queryOptions<
		GetDashboardMetricsQueryResponse,
		ResponseErrorConfig<Error>,
		GetDashboardMetricsQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return getDashboardMetrics({
				...config,
				signal: config.signal ?? signal,
			});
		},
	});
}

/**
 * @summary Get aggregate metrics for the operations panel
 * {@link /api/dashboard/metrics}
 */
export function useGetDashboardMetrics<
	TData = GetDashboardMetricsQueryResponse,
	TQueryData = GetDashboardMetricsQueryResponse,
	TQueryKey extends QueryKey = GetDashboardMetricsQueryKey,
>(
	options: {
		query?: Partial<
			QueryObserverOptions<
				GetDashboardMetricsQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getDashboardMetricsQueryKey();

	const query = useQuery(
		{
			...getDashboardMetricsQueryOptions(config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
