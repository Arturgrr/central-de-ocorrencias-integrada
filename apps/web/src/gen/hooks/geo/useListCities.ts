/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { ListCitiesQueryResponse } from "../../types/geo/ListCities";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { listCities } from "../../clients/geo/listCities";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listCitiesQueryKey = () => [{ url: "/api/cities" }] as const;

export type ListCitiesQueryKey = ReturnType<typeof listCitiesQueryKey>;

export function listCitiesQueryOptions(
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = listCitiesQueryKey();
	return queryOptions<
		ListCitiesQueryResponse,
		ResponseErrorConfig<Error>,
		ListCitiesQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return listCities({ ...config, signal: config.signal ?? signal });
		},
	});
}

/**
 * @summary List cities
 * {@link /api/cities}
 */
export function useListCities<
	TData = ListCitiesQueryResponse,
	TQueryData = ListCitiesQueryResponse,
	TQueryKey extends QueryKey = ListCitiesQueryKey,
>(
	options: {
		query?: Partial<
			QueryObserverOptions<
				ListCitiesQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? listCitiesQueryKey();

	const query = useQuery(
		{
			...listCitiesQueryOptions(config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
