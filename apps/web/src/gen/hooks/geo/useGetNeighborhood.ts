/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	GetNeighborhoodQueryResponse,
	GetNeighborhoodPathParams,
	GetNeighborhood404,
} from "../../types/geo/GetNeighborhood";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { getNeighborhood } from "../../clients/geo/getNeighborhood";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getNeighborhoodQueryKey = ({
	id,
}: {
	id: GetNeighborhoodPathParams["id"] | undefined;
}) => [{ url: "/neighborhoods/:id", params: { id: id } }] as const;

export type GetNeighborhoodQueryKey = ReturnType<
	typeof getNeighborhoodQueryKey
>;

export function getNeighborhoodQueryOptions(
	{ id }: { id: GetNeighborhoodPathParams["id"] | undefined },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getNeighborhoodQueryKey({ id });
	return queryOptions<
		GetNeighborhoodQueryResponse,
		ResponseErrorConfig<GetNeighborhood404>,
		GetNeighborhoodQueryResponse,
		typeof queryKey
	>({
		enabled: !!id,
		queryKey,
		queryFn: async ({ signal }) => {
			return getNeighborhood(
				{ id: id! },
				{ ...config, signal: config.signal ?? signal },
			);
		},
	});
}

/**
 * @summary Get a neighborhood by id
 * {@link /neighborhoods/:id}
 */
export function useGetNeighborhood<
	TData = GetNeighborhoodQueryResponse,
	TQueryData = GetNeighborhoodQueryResponse,
	TQueryKey extends QueryKey = GetNeighborhoodQueryKey,
>(
	{ id }: { id: GetNeighborhoodPathParams["id"] | undefined },
	options: {
		query?: Partial<
			QueryObserverOptions<
				GetNeighborhoodQueryResponse,
				ResponseErrorConfig<GetNeighborhood404>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getNeighborhoodQueryKey({ id });

	const query = useQuery(
		{
			...getNeighborhoodQueryOptions({ id }, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<GetNeighborhood404>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
