/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	SetVehicleStatusMutationRequest,
	SetVehicleStatusMutationResponse,
	SetVehicleStatusPathParams,
	SetVehicleStatus404,
} from "../../types/vehicles/SetVehicleStatus";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	UseMutationOptions,
	UseMutationResult,
	QueryClient,
} from "@tanstack/react-query";
import { setVehicleStatus } from "../../clients/vehicles/setVehicleStatus";
import { mutationOptions, useMutation } from "@tanstack/react-query";

export const setVehicleStatusMutationKey = () =>
	[{ url: "/vehicles/:id/status" }] as const;

export type SetVehicleStatusMutationKey = ReturnType<
	typeof setVehicleStatusMutationKey
>;

export function setVehicleStatusMutationOptions<TContext = unknown>(
	config: Partial<RequestConfig<SetVehicleStatusMutationRequest>> & {
		client?: Client;
	} = {},
) {
	const mutationKey = setVehicleStatusMutationKey();
	return mutationOptions<
		SetVehicleStatusMutationResponse,
		ResponseErrorConfig<SetVehicleStatus404>,
		{
			id: SetVehicleStatusPathParams["id"];
			data: SetVehicleStatusMutationRequest;
		},
		TContext
	>({
		mutationKey,
		mutationFn: async ({ id, data }) => {
			return setVehicleStatus({ id, data }, config);
		},
	});
}

/**
 * @summary Update a vehicle's status
 * {@link /vehicles/:id/status}
 */
export function useSetVehicleStatus<TContext>(
	options: {
		mutation?: UseMutationOptions<
			SetVehicleStatusMutationResponse,
			ResponseErrorConfig<SetVehicleStatus404>,
			{
				id: SetVehicleStatusPathParams["id"];
				data: SetVehicleStatusMutationRequest;
			},
			TContext
		> & { client?: QueryClient };
		client?: Partial<RequestConfig<SetVehicleStatusMutationRequest>> & {
			client?: Client;
		};
	} = {},
) {
	const { mutation = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...mutationOptions } = mutation;
	const mutationKey =
		mutationOptions.mutationKey ?? setVehicleStatusMutationKey();

	const baseOptions = setVehicleStatusMutationOptions(
		config,
	) as UseMutationOptions<
		SetVehicleStatusMutationResponse,
		ResponseErrorConfig<SetVehicleStatus404>,
		{
			id: SetVehicleStatusPathParams["id"];
			data: SetVehicleStatusMutationRequest;
		},
		TContext
	>;

	return useMutation<
		SetVehicleStatusMutationResponse,
		ResponseErrorConfig<SetVehicleStatus404>,
		{
			id: SetVehicleStatusPathParams["id"];
			data: SetVehicleStatusMutationRequest;
		},
		TContext
	>(
		{
			...baseOptions,
			mutationKey,
			...mutationOptions,
		},
		queryClient,
	) as UseMutationResult<
		SetVehicleStatusMutationResponse,
		ResponseErrorConfig<SetVehicleStatus404>,
		{
			id: SetVehicleStatusPathParams["id"];
			data: SetVehicleStatusMutationRequest;
		},
		TContext
	>;
}
