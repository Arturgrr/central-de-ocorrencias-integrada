/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	GetVehicleQueryResponse,
	GetVehiclePathParams,
	GetVehicle404,
} from "../../types/vehicles/GetVehicle";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { getVehicle } from "../../clients/vehicles/getVehicle";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getVehicleQueryKey = ({
	id,
}: {
	id: GetVehiclePathParams["id"] | undefined;
}) => [{ url: "/api/vehicles/:id", params: { id: id } }] as const;

export type GetVehicleQueryKey = ReturnType<typeof getVehicleQueryKey>;

export function getVehicleQueryOptions(
	{ id }: { id: GetVehiclePathParams["id"] | undefined },
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getVehicleQueryKey({ id });
	return queryOptions<
		GetVehicleQueryResponse,
		ResponseErrorConfig<GetVehicle404>,
		GetVehicleQueryResponse,
		typeof queryKey
	>({
		enabled: !!id,
		queryKey,
		queryFn: async ({ signal }) => {
			return getVehicle(
				{ id: id! },
				{ ...config, signal: config.signal ?? signal },
			);
		},
	});
}

/**
 * @summary Get a vehicle by id
 * {@link /api/vehicles/:id}
 */
export function useGetVehicle<
	TData = GetVehicleQueryResponse,
	TQueryData = GetVehicleQueryResponse,
	TQueryKey extends QueryKey = GetVehicleQueryKey,
>(
	{ id }: { id: GetVehiclePathParams["id"] | undefined },
	options: {
		query?: Partial<
			QueryObserverOptions<
				GetVehicleQueryResponse,
				ResponseErrorConfig<GetVehicle404>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getVehicleQueryKey({ id });

	const query = useQuery(
		{
			...getVehicleQueryOptions({ id }, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<GetVehicle404>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
