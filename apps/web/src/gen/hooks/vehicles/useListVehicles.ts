/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	ListVehiclesQueryResponse,
	ListVehiclesQueryParams,
} from "../../types/vehicles/ListVehicles";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/api-client";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import { listVehicles } from "../../clients/vehicles/listVehicles";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listVehiclesQueryKey = (params?: ListVehiclesQueryParams) =>
	[{ url: "/api/vehicles" }, ...(params ? [params] : [])] as const;

export type ListVehiclesQueryKey = ReturnType<typeof listVehiclesQueryKey>;

export function listVehiclesQueryOptions(
	{ params }: { params?: ListVehiclesQueryParams } = {},
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = listVehiclesQueryKey(params);
	return queryOptions<
		ListVehiclesQueryResponse,
		ResponseErrorConfig<Error>,
		ListVehiclesQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return listVehicles(
				{ params: params },
				{ ...config, signal: config.signal ?? signal },
			);
		},
	});
}

/**
 * @summary List vehicles
 * {@link /api/vehicles}
 */
export function useListVehicles<
	TData = ListVehiclesQueryResponse,
	TQueryData = ListVehiclesQueryResponse,
	TQueryKey extends QueryKey = ListVehiclesQueryKey,
>(
	{ params }: { params?: ListVehiclesQueryParams } = {},
	options: {
		query?: Partial<
			QueryObserverOptions<
				ListVehiclesQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? listVehiclesQueryKey(params);

	const query = useQuery(
		{
			...listVehiclesQueryOptions({ params }, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
